# Environment Rules
- You must always run python tasks using the explicit Conda environment binary.
- NEVER use the generic global `python` or `pip` commands.
- Always prepend execution commands or direct terminal commands using: `/home/awech/.conda/envs/dev-alarms/bin/python`
